Dogfooding correction: the missing sw-MLPL text surface (string concatenation, number-to-string, string length/index/search, string-list construction) is a set of BLOCKERS to be fixed upstream and then used, not friction to be worked around permanently. Write docs/mlpl-blockers.md as an implementable specification: verified symptoms with verbatim errors and repros, required builtin signatures and semantics, acceptance tests in MLPL, and what this repository deletes once each lands. Verify every claim against the interpreter before writing it -- including which capabilities already work, so the record is accurate. Add a capability probe that reports open vs closed blockers and register it in the catalog. Re-frame docs/upstream-asks.md and AGENTS.md to point at the blocker doc. Do not modify ../sw-mlpl.