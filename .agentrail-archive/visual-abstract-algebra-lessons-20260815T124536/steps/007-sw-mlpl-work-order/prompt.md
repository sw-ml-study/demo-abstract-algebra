Consolidate every sw-MLPL finding from this repository into a single self-contained handoff document, docs/sw-mlpl-work-order.md, that an agent working ../sw-mlpl can implement from without reading anything else here. Cover: the four bugs (bare-filename CLI exit 1 with the exact site and one-line fix, recursion abort, the comment-span render bug, doc drift), the missing text surface B1-B5, the visualization gaps, the playground narration gaps, and the minor items. For each: verified symptom with a runnable repro, the file in sw-mlpl to change, the proposed signature or behavior, acceptance tests, and what it unblocks downstream. Give a recommended order by value-per-effort, mark bug vs feature honestly, and explain how to verify a fix landed using probes/text_capabilities.mlpl. Do not modify ../sw-mlpl.