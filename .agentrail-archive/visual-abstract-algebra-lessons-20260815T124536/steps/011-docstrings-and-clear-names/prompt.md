Every def u: must carry a docstring and a self-explanatory name. Audit says 14 of 218 documented, which makes the corpus unreadable to the audience it exists for. sw-MLPL's convention is a nonempty string expression as the first statement of the body -- used by the built-in demo catalog and enforced by demo-algorithms/scripts/check-docstrings. Extend scripts/check-docstrings to enforce it here, then document every function and rename the opaque ones (u:nm, u:px, u:ex, u:seg, u:dot, u:box, u:txt, u:col, u:sz, u:mx, u:first, u:second, u:gather2, u:tick and the rest). Docstrings must say what the function ANSWERS, not restate its name; a reader should be able to follow a lesson without reading a body.