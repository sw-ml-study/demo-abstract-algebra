Made the visuals viewable. web/ now holds standalone, include-free bundles generated by scripts/build-web-demos from demos/web/*.mlpl, each ending with the SVG as its final value so the playground draws it in a .svg-output widget; verified against the actual UI predicate (out.starts_with("<svg") in mlpl-web-render-aux/src/entry.rs) by running each bundle through run_script in tests/test_web_entries.mlpl. Added animated SVG via SMIL: u:assoc_walk_svg walks two markers along the two bracketings to different results, verified by seeking a browser to t=3.6s. Added lesson 02 (RPSLS as function, table, and dominance digraph, asserting out/in-degree 2). README now embeds the RPS table, the animation, and the pentagram from a tracked assets/ tree; scripts/check-generated diffs both generated trees so stale artifacts fail the gate. docs/viewing.md covers all five viewing paths and why pasting a lesson shows text. New upstream findings: #9 mlpl-repl with a bare filename exits 1, #10 run_script returns a string result's repr. Gate green: 6 demos, 12 tests.