Fixed the unreadable animations. svg(_, "life") has four compounding deficiencies for a Cayley table -- no axis labels, one colour for all live cells so value cannot carry identity, no caption, and a fixed 0.35s frame -- now written up as ask #12 and mirrored into the work order. Built u:frames_svg in lib/render.mlpl: the full labelled colour-coded table stays on screen while each frame rings the cells it owns, captioned with the element name, at ~1.6s per frame; wired into lesson 02, assets/ and the README. For the web demos, dropped animation entirely -- latin_square.mlpl now shows two static labelled tables where the Latin-square property is visible without motion, and rpsls_pentagon.mlpl keeps just the graph. Corrected my own description of the 5x5 pattern: a hook hinged on the diagonal (tie, two wins along the row, the same two mirrored down the column) sliding one step per frame. Gate green: 6 demos, 12 tests.