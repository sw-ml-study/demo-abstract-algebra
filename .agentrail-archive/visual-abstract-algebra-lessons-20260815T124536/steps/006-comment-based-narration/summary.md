Replaced string-literal narration with asterisk-framed # comment blocks. A string statement echoes twice in the transcript (input line + evaluated output); a comment renders once with no output, verified against the wasm evaluator. Placement does the work: a block before a picture rides with that statement so prose and picture share one entry, and the closing block is a trailing group so the run ends on prose with empty output. Confirmed MLPL has no block-comment syntax (/* */ is a parse error), hence the asterisk bars, recorded as upstream ask #17. Flipped check-comment-style to reject string narration (it previously forbade the comment blocks) and check-narration to require opening/closing bars in the generated file; fixed check-web-renders to count distinct pictures by content hash. build-web-demos now strips only the metadata header, not the narration. CLI lessons use matching asterisk framing. Regenerated web/*.mlpl. Gate green: 6 demos, 12 tests.