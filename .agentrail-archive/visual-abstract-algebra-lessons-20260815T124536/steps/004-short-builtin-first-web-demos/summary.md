Fixed the web demos not showing graphics. Root-caused against the real browser evaluator (wasm WasmSession, plus a faithful JS port of group_statements): bundles produced 89 statement groups, so 88 entries printed 0 and the diagram was buried. Rewrote demos/web to be builtin-first and short: magma_rps 4 groups, latin_square 5, rpsls_pentagon 15, verified in-browser with zero errors and SVG entries rendering. Discovered sw-MLPL ships 12 svg() types but documents 6, omitting svg(frames,"life") which emits a SMIL-animated grid from [T,H,W] -- so the animated Cayley table is now one builtin call via reshape(transpose(one_hot(flatten(t),n)),[n,n,n]), where a group's frames are permutation matrices. Added scripts/check-web-size, an awk port of the browser's grouper, budgeting 20 groups per demo; its counts match the browser exactly. Recorded upstream asks 11-14 (undocumented viz types, life is binary, life clamps small boards tiny, no graph type). Gate green: 6 demos, 12 tests.