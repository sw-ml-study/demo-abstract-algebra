B5 (string-list as a u: argument) and A1 (bare-filename invocation) are live in the current binary, verified directly. Adopt both. In the web demos replace the {names: [...]} record wrappers with plain string lists, which removes record-literal noise from paste-ready files. In lib/render.mlpl the {title, names, table} structure record stays -- a structure IS those three things together -- but re-document it as a deliberate choice rather than a forced workaround, since that distinction was the whole complaint. Drop the bare-filename warning from docs/viewing.md, mark A1 and B5 SHIPPED in the work order, and update the probe and blockers docs.