Replace string-literal narration with COMMENTS. A string statement echoes twice -- once as the input line and again as the evaluated output -- which is the repetition under complaint. A comment renders once, as the input annotation, with no output; verified against the real wasm evaluator: a trailing comment-only group yields OUT[] and renders as prose. MLPL has no block comments (/* */ is a parse error), so frame the blocks with asterisks in the C style the user asked for, using # line comments. Design: prolog block at the top; a short lead-in block immediately before each svg() so the prose and its picture share one entry; epilog block at the end as its own trailing group; terse trailing labels on ordinary code lines; zero string-literal narration. Update scripts/check-narration and scripts/check-comment-style to enforce the new convention (they currently forbid full-line comments, which is now backwards). Record the upstream asks: no block comment syntax, and a leading comment block cannot be its own entry because it rides with the following statement while render_input_line splits the whole group at its first hash.