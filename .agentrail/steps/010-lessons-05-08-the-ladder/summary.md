Lessons 05-08 implemented and green: the ladder from semigroup to group. 05 shows two identity-free semigroups then the S^1 adjoining construction (substitute for the B1/B3-blocked string version, recorded in the header and the plan). 06 finds the identity by search, states uniqueness as an array fact (the mask never holds two bits), and shows the cross owns 2n-1 cells. 07 makes inverses depend on an identity and contrasts Z4's permutation-matrix inverse mask with max's single ring and empty rows. 08 derives the Latin square and verifies the n^2 counting test agrees with the n^3 classifier on every structure here, then distinguishes Z4 from Klein by self-inverse count. Added u:is_latin, u:mask_of_identity, u:diagonal to lib/algebra.mlpl and u:marked_svg to lib/render.mlpl, all tested and guarded against non-closed tables. README carries the identity cross and the Latin square with reading keys. Gate green: 12 demos, 12 tests, docs in sync. Pushed.