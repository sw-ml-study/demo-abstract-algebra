Reframed the missing sw-MLPL text surface from workaround-able friction to specified upstream blockers. Wrote docs/mlpl-blockers.md: six blockers (B1 str_concat/str_join, B2 to_string, B3 str_len/slice/find/split, B4 string-list construction, B5 string-list as u: argument, B6 recursion cap instead of process abort), each with verified symptom, repro, required signatures, acceptance tests, and the bridge it deletes here. Minimum viable set B1+B2+B5. Added probes/text_capabilities.mlpl (registered in catalog, runs under just demos) reporting open vs closed per blocker; verified it detects absent, present-but-wrong, and correct. Corrected the record: to_number/to_int exist and work, so number-from-string is NOT a gap; B2's float precision is mitigable via round(). Found doc drift: lang-reference.md shows a non-running string + example. Re-framed AGENTS.md, README.md, docs/upstream-asks.md, docs/plan.md around the blocker/ask split. Gate green: 2 demos, 7 tests.