Implement lessons 09 and 10 per docs/plan.md. 09: commutativity as a property rather than a rung; S_3 as the non-abelian witness; reflection across the diagonal shown in the viewer. 10: the serious array-programming workload -- enumerate all n^(n^2) binary operations (16 at n=2, 19683 at n=3) and classify every one, then chart the population per rung. Attempt the batched formulation first: all operations as one [n^(n^2), n, n] array via base-n digit extraction, with every law predicate applied along the leading axis. If the associativity cube cannot be lifted to a batch without a per-table loop, ship the loop-based version and record the finding in docs/upstream-asks.md -- that is a real result, not a failure. Expect the largest crop of friction here.