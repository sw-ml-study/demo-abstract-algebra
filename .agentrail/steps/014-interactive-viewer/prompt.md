Build viewer/cayley.html: a dependency-free single-file page that loads a structure JSON emitted by u:write_structure_json, renders the Cayley table, and recomputes every law verdict live as the learner edits a cell. Ship the brief's two puzzles: 'make this magma associative' and 'turn this semigroup into a monoid'. The page re-implements the law checks in JavaScript because it cannot call the interpreter; that duplication must be pinned by tests/test_viewer_conformance.mlpl, which writes the fixture set the page's checker must reproduce. MLPL stays authoritative -- if they disagree, the page is wrong.