Built the structure lattice docs/research2.txt asked for: demos/00-map/structure_lattice.mlpl draws two routes from magma to group with every arrow labelled by the law that earns it, showing that associativity is the first step on one route and the last on the other. Commutativity and idempotence are named in the footer as properties rather than drawn as steps. The map is VERIFIED -- each node carries a real table and the lesson asserts the classifier puts it there -- and it surfaces a real limitation honestly: u:structure_name calls the quasigroup and the loop 'magma' because its ladder only knows the associative branch. Added u:lattice_svg to lib/render.mlpl with edge labels placed 35% along each arrow, since midpoints collide where edges converge. In the README and assets/. Gate green: 18 demos, 16 tests.