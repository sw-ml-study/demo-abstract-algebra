Build the structure map docs/research2.txt asked for: a property LATTICE rather than a single ladder. The associative branch magma-semigroup-monoid-group beside the division branch magma-quasigroup-loop-group, with commutative and idempotent shown as properties that decorate any rung rather than steps on one. Render it as an SVG from MLPL so it stays in the repo's own idiom, put it in the README, and reference it from the lessons that teach each rung.