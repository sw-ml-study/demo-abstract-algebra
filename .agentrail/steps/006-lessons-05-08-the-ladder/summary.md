Narration pass. Every demo now opens with what it will show, annotates its lines tersely, and closes with how to read the picture. Added the beats-cycle diagram (RPS triangle with arrows, RPSLS pentagram with arrowheads and labels) as the LEAD visual in the web demos, lesson 01, and the README, with the Cayley table revealed as the same object. Matched sw-MLPL's comment style after measuring the catalog (71% of lines annotated, labels not sentences): trailing comments now capped at 52 chars by scripts/check-comment-style, which also forbids full-line comments in web demos because render_input_line splits a whole statement group at its first # and hides the code. CLI lessons print a HOW TO READ WHAT WAS JUST WRITTEN section per artifact via new lib helpers. scripts/check-narration enforces prolog/epilog structure; scripts/check-web-renders proves each demo draws (1,2,2, matching the browser). Recorded asks #15 (a pasted file cannot produce a narration panel, so prolog/epilog cannot match the house intro/takeaway styling from a companion repo) and #16 (multi-line comment split hides code). Gate green: 6 demos, 12 tests.