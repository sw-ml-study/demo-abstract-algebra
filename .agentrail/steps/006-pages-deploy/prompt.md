Put something on a public URL. Saga A step 6 of docs/tutorial-plan.md, pulled forward because nothing in this repo is currently clickable: there is no ./pages, no .github, and viewer/cayley.html is a local file. Compose a site from what already exists and works -- the Lab (viewer/cayley.html, promoted), the runtime spike as live evidence the interpreter runs in a page, and an index that presents the four Diataxis doors HONESTLY, naming what is built and what is not. Do not fake a course that has no lessons. The sw-MLPL wasm bundle is fetched at deploy time from a pinned mlpl-live ref and never committed. Add .github/workflows/pages.yml, scripts/build-site, a just recipe, and link the URL from the README.