Deployed the site. scripts/build-site composes out/site from existing artifacts (index with the four Diataxis doors, /lab = viewer/cayley.html promoted, /spike = the runtime spike, /mlpl = the pinned playground bundle); scripts/serve-site serves it under a /demo-abstract-algebra/ prefix matching GitHub Pages; .github/workflows/pages.yml checks out sw-ml-study/mlpl-live at the ref pinned in site/BUNDLE_REF and deploys. The 8MB wasm is never committed. Found and fixed a real deployment blocker: the playground index.html imports its glue from ABSOLUTE paths, which cannot resolve under a Pages project prefix -- three references are rewritten to relative and the build fails if an absolute one survives. Verified in headless Chrome under the real prefix: six routes 200, index and Lab render with no console errors, spike passes every case. The index names what is built and what is not; Learn stays under WHAT IS NOT BUILT YET until Saga B. README links the site from the top and from Quick start. Committed and pushed.