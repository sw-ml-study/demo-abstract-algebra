Shipped the course. learn/lessons/course.mlpl holds 11 lessons in 3 modules (Operations, Closure, Rock-paper-scissors) as MLPL records; scripts/build-learn generates learn/lessons.json; learn/index.html is the runner (prompt, editor, Run, Cayley render, verdict with witness, hints, progress). lib/grade.mlpl gained u:grade_differs so a counterexample exercise grades the SEARCH not one blessed answer. scripts/check-learn is a new gate that drives the real page -- types each lesson's solution, clicks every choice on a prediction -- and fails unless all lessons complete AND every prediction has exactly one right answer. Verified 11/11 both locally and against the deployed site. Also fixed the two things Mike reported: contrast (body prose was 2.76:1, below WCAG AA; now 11.6:1 gold on near-black, headings 15.4:1, everything clears AAA) and the beginner-hostile index (it opened on magmas and the structure lattice; now opens with what a visitor learns, poses the five laws as questions in English, and names magma/semigroup/monoid/group only after you would need them). Dogfooding found ask #25: array literals cannot hold records, so a list of lessons is impossible -- the course is a record keyed by slug plus an order list, and build-learn validates the two agree. Six lessons appeared broken during development purely because the browser cached a stale lessons.json -- the third stale-signal false pass this session. Gate green: 20 demos, 24 tests, web-runs 6/6, check-learn 11/11. Committed, pushed, deployed, live.