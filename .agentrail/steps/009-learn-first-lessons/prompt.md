Saga A steps 3-5 and Saga B module 0-1, together, because the site currently shows scaffolding and no teaching. Write lib/grade.mlpl -- every checker answers the SAME record {pass, why, witness}, and a failure names a witness rather than returning 0 -- with tests/test_grade.mlpl asserting each against a known-good and a known-bad submission. Author lessons as MLPL records (dogfooding) and generate learn/lessons.json from them. Build the course runner page: prompt, editor, Run, result, Cayley render, grade, hints, next. Ship the first module end to end. Also fix the site index for its actual audience -- it opens on magmas and the structure lattice, which is meaningless to a newcomer, and its body text fails contrast. Lead with what a visitor will learn and why they should care.