Close the three cheap items from docs/research2.txt before Stage 2 multiplies the rendering code. (1) Mark rendering plumbing as infrastructure in the generated web demos with a banner saying nothing below this line is needed to define the algebra, and emit the mathematics before the graphics. (2) Rename opaque renderer parameters -- a, b, k, f, t, acc, i -- to from_element, to_element, element_count, fraction, svg_so_far and similar, in lib/render.mlpl and the inline web emitters. (3) Distinguish docstring registers: prefix renderer docstrings with 'SVG helper:' so a novice has permission to skip them. Add checks where they can be checked.