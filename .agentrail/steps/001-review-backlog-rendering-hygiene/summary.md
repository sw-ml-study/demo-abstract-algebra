Closed the rendering-hygiene backlog from docs/research2.txt before Stage 2 multiplies the rendering code. (1) RENDERING SUPPORT banners in lib/render.mlpl and all three generated web demos, with the algebra emitted first -- 8 to 10 helpers moved below the line per file. (2) Renamed every opaque renderer parameter across lib and the web emitters: u:along_x(a,b,k,f) became u:along_x(from_element, to_element, element_count, fraction). (3) Split docstring registers -- renderers say 'SVG helper:', mathematical functions say what they answer -- enforced by scripts/check-docstrings. Also moved u:row_beats_column out of the rendering section into lib/algebra.mlpl, where it belongs. Two of my own transform errors were caught by existing checks rather than by me: a rename that edited inside a string literal (captions read 'panel = 0' instead of 'a = 0', caught by check-generated) and a first attempt that renamed only the head line of multi-line bodies (caught by just demos). Gate green: 17 demos, 16 tests.