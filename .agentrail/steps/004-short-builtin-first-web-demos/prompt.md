The web bundles do not show graphics in practice and are indefensibly long. Two root causes, both measured against the real browser evaluator. (1) A bundle splices the whole library, so the editor's group_statements yields 89 statement groups -- 89 REPL entries, 88 of them printing 0, with the picture at the very bottom. (2) The SVG was hand-rolled when sw-MLPL already ships 12 svg() types including svg(frames, "life"), which emits a SMIL-animated grid from a [T,H,W] array in ONE call. Rewrite demos/web/* to be short and builtin-first: use svg(t, "heatmap") for the table and svg(reshape(transpose(one_hot(flatten(t), n)), [n,n,n]), "life") for the animated per-element frames, where a group's frames are each a permutation matrix -- the Latin square, animated, with no hand-written SVG. Target well under 15 statement groups per demo. Verify every demo through the real wasm evaluator with a faithful port of group_statements, asserting group count and that SVG entries render. Record the findings: docs/lang-reference.md documents 6 of 12 svg types and omits life, heatmap_grid, waffle, scatter3d, plotly3d, and attention_overlay; and svg(_, "life") is binary alive/dead so it cannot carry element identity.