Saga A step 2 of docs/tutorial-plan.md. Extract the proven bridge out of the spike into learn/runtime.js: a small MlplRuntime interface (ready, eval, evalJson, reset) with the hidden same-origin iframe as its ONLY implementation, so that adopting a headless build later is a one-file swap. The spike page becomes a consumer of it rather than a copy of it, and must still pass 8/8 on the deployed site. Fold in what the spike learned the hard way: to_json answers a Result so the display form arrives wrapped as Ok(...) and must be unwrapped at the grading boundary, and the browser has no include so lib/algebra.mlpl is fetched and evaluated as source after every reset. Blocker B7 already names this file as the bridge to delete.