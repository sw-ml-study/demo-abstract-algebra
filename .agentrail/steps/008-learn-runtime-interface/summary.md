Extracted learn/runtime.js: the MlplRuntime seam (ready, eval, evalJson, prelude, reset) with the hidden same-origin iframe as its only implementation, so adopting B7's headless build is a one-file swap. The spike is now a CONSUMER of it and never mentions an iframe. Two hard-won behaviours became documented library code: unwrapResult (to_json answers a Result so JSON arrives as Ok({...}) -- ask #10 at a second boundary, #20) and isFailure (a failure is error: OR Err(, because write_text/read_text/run_script answer Err rather than erroring). prelude/reset handle the missing include (#21) by re-evaluating library source after a clear, so a reset drops the learner's work but keeps the library -- asserted by a new case. Spike now 10/10 locally, verified after the refactor. Gate green. Committed and pushed.