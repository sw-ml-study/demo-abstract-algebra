Write docs/sw-mlpl-bug-report.md: a standalone, reproducible defect report for the sw-MLPL team, filed against the interpreter version actually in use. The headline is the 1x1 rank collapse regression found while running the gate (upstream ask #22). Characterize it properly -- which shapes, which operations, which direction -- rather than restating the one symptom. Re-verify every previously recorded DEFECT against the current binary and say which still reproduce and which are fixed; AGENTS.md requires claims be checked against the interpreter, and the recorded ones were written against an older build. Bugs only: feature absences stay in docs/upstream-asks.md and docs/mlpl-blockers.md. Link it from the README.