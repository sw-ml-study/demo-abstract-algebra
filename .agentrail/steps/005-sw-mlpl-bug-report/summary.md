Wrote docs/sw-mlpl-bug-report.md, linked from the README. Re-ran every previously recorded DEFECT against mlpl-repl 0.20.0 rather than restating docs written against an older build: three still reproduce (run_script string rendering #10; deep recursion abort B6, exit 134; string lists cannot be built B4), three are fixed (ge/le now exist, str_concat, to_string, plus the lang-reference example that could not run). Characterised the new regression properly -- it is NOT 1x1 as first recorded but ANY all-unit shape at any rank ([1], [1,1], [1,1,1] all collapse to []), only under scalar broadcasting (*, +, -, /, eq, lt), while array-array ops, transpose, reshape and flatten are correct; shape([[0]] * [1]) is [1], which points at the broadcast path losing unit axes down to the other operand's rank. Corrected ask #22 and the lib/algebra.mlpl bridge comment to that wider scope. Noted that B4 is an easy false positive: str_join on a LITERAL list succeeds, which looks like a fix and is not. Gate green: 20 demos 0 failures, 17 tests 17 passed. Committed; NOT pushed.