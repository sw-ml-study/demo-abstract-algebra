Re-scoped 004 from 'three more paste-into-playground .mlpl files' to the question those files could not answer: can a page we control run learner-written MLPL? Proved yes. learn/spike/runtime-spike.html passes 7/7 in Chrome -- a hidden same-origin iframe of the playground bundle hands the parent a WasmSession in ~100ms; all 810 lines of lib/algebra.mlpl load as source and classify a page-built table; an MLPL checker grades good and bad submissions with a witness. Wrote docs/tutorial-plan.md: runtime decision with two implementations behind one MlplRuntime interface, MLPL-authored semantic grading, lesson records as MLPL, the Diataxis split, an animation policy (animate a process not a pattern; an animation is an answer not a decoration), and seven sagas with A and B specified step by step. Opened blocker B7 (no headless WASM build published) naming learn/runtime.js as the bridge it deletes, plus asks #20 (eval_with_values not exposed; to_json arrives wrapped as Ok(...)) and #21 (no include in a browser session). ALSO: the gate was already red at HEAD -- mlpl-repl 0.20.0 is two weeks newer than the last commit and collapses any 1x1 array to rank 0 under scalar broadcasting, crashing u:invertible_mask on the trivial group. Recorded as ask #22 with a minimal repro and bridged with a reshape; suite green again (20 demos 0 failures, 17 tests 17 passed). Committed 0c7bc06; NOT pushed -- see notes.