Implement lessons 05-08 per docs/plan.md: semigroup (associative, no identity), monoid (identity discovered not asserted, plus its uniqueness), inverses, and groups (Z_n and the Klein four-group). Add the Latin-square check and a side-by-side comparison renderer so a group and a non-group can be shown together. This is where the two teaching pictures must land: the identity's white cross and the Latin square. Update the catalog, add conformance tests, append friction.