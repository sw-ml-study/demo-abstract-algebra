Adopted str_join and to_string the day they landed and deleted the bridges they replace: u:join/u:join3/u:join5 (byte round-trip) and u:number_text (to_json lean), from lib/render.mlpl and all three web demos. Converted 66 call sites with a balanced-paren transform, then flattened the nesting str_join made unnecessary -- an SVG element now reads as the list of fragments it is, in one linear-time call instead of an O(n^2) pairwise fold. Retargeted the string test at the builtins including to_string(8/2)=='4'; updated the probe, mlpl-blockers.md, upstream-asks.md, the work order and the README. 12 demos, 14 tests green; web demos shrank to 16/17/15 groups; 208 functions all documented. Also corrected my own earlier claim that the binary was stale -- it had been rebuilt, and I had inferred from mtime instead of calling str_concat.