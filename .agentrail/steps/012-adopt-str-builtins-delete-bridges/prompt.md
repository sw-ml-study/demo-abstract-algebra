Upstream landed B1 (str_concat/str_join) and B2 (to_string) and the adjacent binary is rebuilt; probes/text_capabilities.mlpl reports both CLOSED. Delete the bridges they justified: u:join/u:join3/u:join5 and u:number_text in lib/render.mlpl, and the inline u:join/u:number_text in each demos/web file. Prefer str_join with a list literal over nested pairwise joins -- it is one linear-time call and reads as the fragment list it is, which collapses the worst code in the repo. Update the probe's load-bearing bridge list, the string-primitives test, docs/mlpl-blockers.md, docs/upstream-asks.md, docs/sw-mlpl-work-order.md and any README claim about missing string primitives. Web demos should get shorter as a side effect.