Every demo now explains its own picture. Adopted the intro/annotated-lines/takeaway shape sw-MLPL's own demo catalog uses, expressed inside a pasteable file: a "WHAT THIS SHOWS" prolog as the first statement, a trailing # annotation on every code line, and "HOW TO READ"/"THE POINT" epilog strings that name what the rows, columns, colors and moving markers actually mean and what to notice. CLI lessons get u:prolog/u:epilog_open/u:explain/u:note/u:takeaway helpers and print a HOW TO READ WHAT WAS JUST WRITTEN section per artifact. Consequence: a web demo's final value is now prose, not an SVG -- which is the correct contract, since the playground renders any entry starting with <svg wherever it sits. Replaced the wrong test with scripts/check-web-renders, which evaluates growing statement-group prefixes (prefix k's value is what entry k displays) and counts rendered entries: 1, 2, 2, matching the browser harness exactly. Added scripts/check-narration to enforce the structure. build-web-demos now strips the source header so the generated file opens on its prolog. Gate green: 6 demos, 12 tests.