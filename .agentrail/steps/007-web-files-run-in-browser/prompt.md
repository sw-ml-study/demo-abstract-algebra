The web/*.mlpl files are advertised as paste-into-the-playground demos and at least one fails there with statements the browser session does not support. tests/test_web_entries.mlpl cannot catch this: it uses run_script, which is the CLI, so it passes on constructs the browser has never had. Reproduce the failures against the REAL browser session, then add a gate that runs every web/*.mlpl (and demos/web/*.mlpl) through a headless WasmSession and fails on any error. Fix the files it catches. This is the check that docs/upstream-asks.md and the memory note about run_script proving the wrong thing both predicted was missing.