Added scripts/check-web-runs (in just audit, ~4s): a static guard that no script-mode construct appears in web/ or demos/web/, plus a runtime guard that evaluates every file in a REAL browser against the REAL interpreter, group by group. The runtime guard exists because tests/test_web_entries.mlpl uses run_script -- the CLI -- and so passes on constructs the browser never had. Found the actual trap: only include errors outright; write_text, read_text and run_script answer Err(... on this surface), so a broken demo looks green to anything that greps for 'error'. The gate fails on error: AND Err(. All six files pass; nothing proved that before. Chrome --dump-dom was tried first to avoid a node dependency and does not work -- virtual time races the wasm boot -- so the harness is driven over CDP; the check skips loudly without Chrome/node/bundle. ALSO adopted upstream's fix: sw-mlpl@7b4545f2 fixed BUG 1 the same day it was filed; verified all five acceptance cases against the rebuilt binary, DELETED the reshape bridge from lib/algebra.mlpl, closed ask #22, updated the bug report. BUG 2 and BUG 3 still reproduce. Committed and pushed.